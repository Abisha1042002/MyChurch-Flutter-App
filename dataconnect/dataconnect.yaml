specVersion: "v1beta"
serviceId: "mychurch"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "mychurch-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
